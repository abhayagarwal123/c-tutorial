//v is the orignal vector contain duplicate element
//v1 not contain duplicate element
//sort before starting the algo
/*
sort(v.begin(),v.end());
      for(int i=0;i<v.size();i++){ 
        if((i+1)<v.size())
        {
              if(v[i]==v[i+1]){continue;}
        }
          v1.push_back(v[i]);
         
      }
      */